<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Survey Completed | <%= survey.title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f8f9fa;
      padding: 20px;
    }
    .success-container {
      max-width: 800px;
      margin: 0 auto;
      background-color: #fff;
      border-radius: 10px;
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
      padding: 30px;
      text-align: center;
    }
    .success-icon {
      font-size: 4rem;
      color: #28a745;
      margin-bottom: 20px;
    }
    .success-title {
      font-size: 2rem;
      margin-bottom: 20px;
      color: #333;
    }
    .success-message {
      font-size: 1.2rem;
      margin-bottom: 30px;
      color: #666;
    }
    .btn-return {
      margin-top: 20px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="success-container">
      <div class="success-icon">
        <i class="bi bi-check-circle-fill"></i>
        <!-- Fallback for Bootstrap Icons -->
        <svg xmlns="http://www.w3.org/2000/svg" width="64" height="64" fill="currentColor" class="bi bi-check-circle-fill" viewBox="0 0 16 16">
          <path d="M16 8A8 8 0 1 1 0 8a8 8 0 0 1 16 0zm-3.97-3.03a.75.75 0 0 0-1.08.022L7.477 9.417 5.384 7.323a.75.75 0 0 0-1.06 1.06L6.97 11.03a.75.75 0 0 0 1.079-.02l3.992-4.99a.75.75 0 0 0-.01-1.05z"/>
        </svg>
      </div>
      
      <h1 class="success-title">Thank You!</h1>
      
      <p class="success-message">
        <%= survey.settings?.thankYouMessage || 'Your survey response has been submitted successfully.' %>
      </p>
      
      <% if (survey.settings?.showResponseId !== false) { %>
        <div class="response-id">
          <small class="text-muted">Response ID: <%= responseId %></small>
        </div>
      <% } %>
      
      <% if (survey.settings?.showReturnButton !== false) { %>
        <div class="btn-return">
          <a href="<%= survey.settings?.returnUrl || '/' %>" class="btn btn-primary">
            <%= survey.settings?.returnButtonText || 'Return to Home' %>
          </a>
        </div>
      <% } %>
    </div>
  </div>
</body>
</html>
